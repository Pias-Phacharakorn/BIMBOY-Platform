# Moved

The canonical version of this guide lives in [`.claude/docs/backend.md`](../../.claude/docs/backend.md).

This `.agents/` copy is a pointer only — do not edit it. Update the `.claude/docs/` file instead.
