# Moved

The canonical version of this guide lives in [`.claude/docs/frontend.md`](../../.claude/docs/frontend.md).

This `.agents/` copy is a pointer only — do not edit it. Update the `.claude/docs/` file instead.
