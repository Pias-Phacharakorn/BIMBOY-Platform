# Moved

The canonical version of this guide lives in [`.claude/docs/drawing.md`](../../.claude/docs/drawing.md).

This `.agents/` copy is a pointer only — do not edit it. Update the `.claude/docs/` file instead.
