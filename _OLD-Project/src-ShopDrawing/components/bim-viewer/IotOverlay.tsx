// IoT overlay stub — RITTA CONNXT viewer disables the IoT module by default.
// Component kept for source-level compatibility with the ported viewer code.
export function IotOverlay() {
  return null;
}
