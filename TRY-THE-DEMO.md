# ลองใช้ BIMBOY — Try the Demo

**เปิดลิงก์นี้ได้เลย ไม่ต้องสมัคร ไม่ต้องติดตั้งอะไร:**

### 👉 https://bimboy-platform.pias-phacharakorn.workers.dev/demo

รบกวนช่วยลองเล่นสัก 10–15 นาที แล้วบอกผมว่าอะไรพัง อะไรงง อะไรช้าครับ 🙏

---

## นี่คืออะไร / What this is

**BIMBOY** คือแพลตฟอร์มจัดการงาน BIM บนเว็บ — รวมโมเดล เอกสาร งาน coordination และข้อมูล GIS
ไว้ที่เดียว เปิดผ่าน browser ล้วน ๆ ไม่ต้องลง Navisworks / Revit

ลิงก์ข้างบนคือ **Guest mode** — โหมดดูอย่างเดียว (read-only):

- ไม่ต้องมี account ไม่ต้อง login
- โหลดโมเดลตัวอย่าง 8 ไฟล์ (federated model) ให้อัตโนมัติ
- แก้อะไรไม่ได้ และไม่มีอะไรถูกบันทึกกลับ — เล่นได้เต็มที่ ไม่ต้องกลัวพัง

---

## เริ่มยังไง / Getting started

1. เปิดลิงก์ด้วย **Chrome หรือ Edge บนคอมพิวเตอร์** (มีเมาส์จะสะดวกกว่ามาก)
2. รอโมเดลโหลด — มี 8 ไฟล์ รวม **~11 MB** จะขึ้นหน้าต่างแสดงความคืบหน้า
   (กด **Stop** ได้ถ้าอยากหยุด) ครั้งแรกช้าหน่อยเพราะยังไม่มี cache
3. พอโมเดลขึ้นแล้ว — เล่นได้เลย

> **มือถือ/แท็บเล็ตก็เปิดได้** แต่ยังไม่ได้ปรับ UI มาเพื่อจอเล็ก ถ้าเจอปัญหาบนมือถือ
> บอกได้ครับ แต่ยังไม่ใช่สิ่งที่ผมเน้นตอนนี้

---

## ลองอะไรดี / Worth clicking

ไม่ต้องทำตามลำดับ กดเล่นตามใจได้เลย — ผมอยากรู้ว่า **คนที่ไม่เคยเห็นระบบนี้ จะกดถูกไหม**

| ลอง | อยู่ตรงไหน |
|-----|-----------|
| **หมุน / เลื่อน / ซูม** — ซูมจะพุ่งเข้าหาจุดที่เคอร์เซอร์ชี้ และหยุดที่ผิววัตถุ (คล้าย Navisworks) | ในวิวพอร์ต |
| **คลิกชิ้นงาน** ดู properties ของ element นั้น | คลิกที่โมเดล → พาเนล **Item Properties** ด้านขวา |
| **เปิด/ปิดโมเดลแต่ละไฟล์**, ซ่อน/แสดง, isolate | พาเนล **Models List** ด้านซ้าย |
| **วัดระยะ** — Length / Area / Surface มี snap เข้าจุดยอด | แถบเครื่องมือ **ขวา** → Measure |
| **ตัดโมเดล (Section)** — คลิกบนผิวเพื่อวางระนาบตัด แล้วลาก gizmo เลื่อนระนาบ | แถบเครื่องมือ **ขวา** → Clip |
| **Section Box** — ครอบกล่องแล้วดูเฉพาะข้างใน | แถบเครื่องมือ **ขวา** → Sectionbox |
| **ดูพิกัด** ของจุดที่คลิก | แถบเครื่องมือ **ขวา** → Coordinate |
| **2D Views** — เปิดแปลนที่มากับโมเดล | พาเนลซ้าย → **2D Views** |
| **GIS** — วางโมเดลบนแผนที่โลกจริง (โปรเจกต์ตัวอย่างปักหมุดที่กรุงเทพฯ) | แท็บ **GIS** ด้านบน |
| **ตั้งค่าวิวพอร์ต** — grid, performance / scene diagnostics | แถบเครื่องมือ **ล่าง** → Settings |

**สิ่งที่ผมอยากรู้ที่สุด:**

1. เครื่องมือไหน **หาไม่เจอ** หรือ **กดแล้วไม่เป็นอย่างที่คิด**
2. ตรงไหน **ช้า / กระตุก** — เครื่องคุณสเปกประมาณไหน
3. คำในเมนู **เรียกถูกไหม** ตรงกับที่ใช้กันในงานจริงหรือเปล่า
4. ถ้าเป็นโปรเจกต์ของคุณจริง ๆ — **ขาดอะไรถึงจะใช้ทำงานได้**

---

## ส่วนที่ยังว่างอยู่ / Empty on purpose

เมนูซ้ายมีครบทุกหัวข้อ แต่ในโหมด demo **ยังไม่มีข้อมูลตัวอย่าง** ในส่วนเหล่านี้ —
เปิดแล้วว่างเปล่าถือว่าปกติ ไม่ต้องแจ้ง:

- **Clash Detection** — ตัวระบบทำงานแล้ว แต่ demo ยังไม่ได้ผูกไฟล์ BCF ตัวอย่างไว้
- **Document Status** / **Drawing** / **PowerBI** — เหมือนกัน คือยังไม่มี data ให้ guest
- **Project Standard**
- **Settings** — ซ่อนไว้สำหรับ guest โดยตั้งใจ (เหมือน member ที่ไม่ใช่ admin)
- **แท็บ AR** — ต้องดึงโมเดลจาก cloud storage ซึ่ง guest เข้าไม่ถึง ยังไม่ต้องลอง

> **แต่ถ้ากดแล้ว error / จอขาว / ค้าง → อันนั้นแจ้งเลยครับ** ว่างได้ แต่พังไม่ได้

---

## รู้แล้ว ไม่ต้องแจ้ง / Already known

- **Models List ขึ้น 7 จาก 8 โมเดลตอนเปิดครั้งแรก** และแต่ละรอบหายไม่ซ้ำไฟล์กัน —
  ไฟล์โหลดครบและแสดงผลครบ เป็นปัญหาที่รายการฝั่ง UI กำลังตามแก้อยู่
- **โหลดครั้งแรกช้า** (~11 MB) — โดยเฉพาะบน 4G/5G รอบต่อไปจะเร็วขึ้นเพราะ cache
- **ปิดแท็บแล้ว session จบ** ต้องเข้าลิงก์ใหม่ — แต่ **กด refresh ยังอยู่ในโหมด demo** ปกติ
  (ถ้า refresh แล้วเด้งออกไปหน้า login → อันนี้ผิด แจ้งด่วน)
- **Grid ปิดไว้เป็นค่าเริ่มต้น** เปิดได้ที่ Settings ในแถบเครื่องมือล่าง

---

## แจ้งกลับยังไง / How to report

**ส่งกลับมาในแชทนี้ได้เลย** พร้อม **screenshot** (ช่วยได้มากที่สุด) แล้วเขียนสัก 3 บรรทัด:

```
กดอะไร     : เปิดแท็บ GIS แล้วกดปุ่ม Save
คิดว่าจะได้ : โมเดลไปวางบนแผนที่
ได้จริง     : ปุ่มกดไม่ติด ไม่มีอะไรเกิดขึ้น

เครื่อง/บราวเซอร์ : Windows 11, Chrome, โน้ตบุ๊ก RTX 3050
```

ถ้าเจอ error ในหน้าจอ เปิด **F12 → Console** แล้วแคปมาด้วยจะช่วยได้เยอะครับ

**ความเห็นแบบ "ใช้แล้วรู้สึกงง" ก็มีค่ามาก** ไม่ต้องรอให้เป็นบั๊ก —
"หาปุ่มนี้ไม่เจอ" หรือ "นึกว่ามันจะทำอีกอย่าง" คือสิ่งที่ผมอยากได้พอ ๆ กับ error ครับ

ขอบคุณมากครับ 🙏
