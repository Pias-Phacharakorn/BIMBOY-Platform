# CONTEXT

_Staging buffer for in-flight design decisions (grill-with-docs). Once a
decision is implemented and promoted into its domain guide under `.agents/docs/`
and `.claude/docs/`, clear it from here — the domain guides are the permanent
record._
